package s3tool.jdog.domain;

import com.silrais.toolkit.dataset.SimpleDataSet;

public class ReferentialKeyList extends SimpleDataSet {
    public ReferentialKeyList(String id) {
        super(id);
    }
}
